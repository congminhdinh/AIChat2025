Act as a Senior DevOps & .NET Developer.

i have updated task 2, see and make changes
**Task 2: Update Dockerfiles (Root Context & Auto-Migration)**
Modify the `Dockerfile` for each service (e.g., `Services/AccountService/Dockerfile`) with the following logic:

1.  **Build Context Adjustment:**
    * Assume the `docker build` command runs from the **Solution Root**.
    * Update `COPY` and `RUN dotnet restore` paths to reflect the directory structure (e.g., `COPY ["Services/AccountService/AccountService.csproj", "Services/AccountService/"]`).

2.  **Migration Bundle Generation (Build Stage):**
    * Install the EF Core tool: `RUN dotnet tool install --global dotnet-ef`
    * Add the tools path to PATH.
    * Generate the migration bundle: `RUN dotnet ef migrations bundle -c <DbContextName> -o /app/efbundle`

3.  **Entrypoint Logic (Runtime Stage):**
    * Copy the `efbundle` from the build stage.
    * Create an `entrypoint.sh` file **directly inside the Dockerfile** (using `RUN echo ...` or similar) with this logic:
        ```bash
        #!/bin/bash
        ./efbundle
        dotnet <ServiceName>.dll
        ```
    * Make the script executable (`chmod +x`).
    * Set the `ENTRYPOINT` to `["./entrypoint.sh"]`.

**Output:**
1.  Confirm the AppSettings updates.
2.  Provide the full, corrected code for **one representative Dockerfile** (e.g., AccountService) explaining the Root Context paths and the Bundle/Entrypoint logic.
3.  Apply similar changes to all other C# service Dockerfiles.