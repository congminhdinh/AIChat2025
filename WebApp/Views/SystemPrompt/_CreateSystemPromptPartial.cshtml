<div class="create-modal-container">
    <div class="modal-header border-0 pb-0">
        <h5 class="modal-title fw-bold">
            <i class="bi bi-plus-circle-fill text-primary me-2"></i>Tạo prompt hệ thống mới
        </h5>
        <button type="button" class="btn-close-modal" onclick="closeCreateModal()">
            <i class="bi bi-x-lg"></i>
        </button>
    </div>

    <div class="modal-body">
        <p class="text-muted small mb-4">
            Thiết lập prompt hệ thống để điều chỉnh hành vi của AI chatbot.
        </p>

        <form id="createSystemPromptForm">
            <div class="row g-3 mb-3">
                <div class="col-md-12">
                    <label for="createSystemPromptName" class="form-label fw-semibold text-secondary small">
                        <i class="bi bi-card-heading me-1"></i>Tên <span class="text-danger">*</span>
                    </label>
                    <input type="text" class="form-control bg-light border-0 py-2"
                           id="createSystemPromptName" name="Name"
                           placeholder="Nhập tên prompt..." required>
                </div>
            </div>

            <div class="row g-3 mb-3">
                <div class="col-md-12">
                    <label for="createSystemPromptContent" class="form-label fw-semibold text-secondary small">
                        <i class="bi bi-file-text-fill me-1"></i>Nội dung <span class="text-danger">*</span>
                    </label>
                    <textarea class="form-control bg-light border-0 py-2"
                              id="createSystemPromptContent" name="Content"
                              rows="6"
                              placeholder="Nhập nội dung prompt..." required></textarea>
                    <div class="form-text small text-muted">
                        <i class="bi bi-info-circle me-1"></i>Nội dung này sẽ được gửi cho AI để điều chỉnh cách phản hồi.
                    </div>
                </div>
            </div>

            <div class="row g-3 mb-3">
                <div class="col-md-12">
                    <label for="createSystemPromptDescription" class="form-label fw-semibold text-secondary small">
                        <i class="bi bi-chat-left-text-fill me-1"></i>Mô tả (Tùy chọn)
                    </label>
                    <textarea class="form-control bg-light border-0 py-2"
                              id="createSystemPromptDescription" name="Description"
                              rows="3"
                              placeholder="Nhập mô tả về mục đích sử dụng..."></textarea>
                </div>
            </div>

            <div class="row g-3 mb-3">
                <div class="col-md-12">
                    <label for="createIsActive" class="form-label fw-semibold text-secondary small">
                        <i class="bi bi-toggle-on me-1"></i>Trạng thái
                    </label>
                    <select class="form-select bg-light border-0 py-2" id="createIsActive" name="IsActive">
                        <option value="false" selected>Không hoạt động</option>
                        <option value="true">Đang hoạt động</option>
                    </select>
                    <div class="form-text small text-muted">
                        <i class="bi bi-lightbulb me-1"></i>Chỉ các prompt đang hoạt động mới được áp dụng.
                    </div>
                </div>
            </div>
        </form>
    </div>

    <div class="modal-footer border-0 justify-content-center pt-0">
        <button type="button" class="icon-btn" onclick="closeCreateModal()" data-bs-toggle="tooltip" title="Hủy">
            <i class="ti ti-x"></i>
        </button>
        <button type="button" class="icon-btn btn-primary" id="btnSubmitCreateSystemPrompt" data-bs-toggle="tooltip" title="Tạo mới">
            <i class="ti ti-check"></i>
        </button>
    </div>
</div>

<style>
    .create-modal-container {
        padding: 5px;
    }

    .modal-header {
        padding: 20px 25px;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .modal-title {
        font-size: 1.25rem;
        color: #2c3e50;
    }

    .btn-close-modal {
        background: none;
        border: none;
        font-size: 1.2rem;
        color: #adb5bd;
        transition: 0.2s;
        cursor: pointer;
    }

    .btn-close-modal:hover {
        color: #495057;
        transform: rotate(90deg);
    }

    .modal-body {
        padding: 0 25px 20px 25px;
        max-height: 70vh;
        overflow-y: auto;
    }

    .modal-body textarea {
        min-height: 150px;
        resize: vertical;
    }

    .form-control:focus, .form-select:focus {
        background-color: #fff !important;
        box-shadow: 0 0 0 0.25rem rgba(13, 110, 253, 0.15);
        border: 1px solid #0d6efd !important;
    }

    .form-label {
        margin-bottom: 8px;
    }

    .modal-footer {
        padding: 15px 25px 25px 25px;
        gap: 10px;
    }

    .btn {
        font-weight: 500;
        transition: all 0.2s;
    }

    .btn-light:hover {
        background-color: #e9ecef;
    }

    .btn-primary:hover {
        background-color: #0b5ed7;
        box-shadow: 0 4px 12px rgba(13, 110, 253, 0.3);
    }

    .icon-btn {
        width: 40px;
        height: 40px;
        padding: 0;
        border-radius: 50%;
        border: 1px solid #dee2e6;
        cursor: pointer;
        transition: all 0.2s;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        background-color: #f8f9fa;
        color: #495057;
    }

    .icon-btn:hover {
        transform: scale(1.1);
        box-shadow: 0 2px 8px rgba(0,0,0,0.15);
    }

    .icon-btn.btn-primary {
        background-color: #0d6efd;
        color: white;
        border-color: #0d6efd;
    }
</style>
